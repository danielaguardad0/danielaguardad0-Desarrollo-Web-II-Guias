import { Montserrat } from 'next/font/google';
import { Lusitana } from 'next/font/google';
 
export const aña = Montserrat({ subsets: ['latin'] });
export const lusitana = Lusitana({
    subsets: ['latin'],
    weight: ['400', '700'],
  });